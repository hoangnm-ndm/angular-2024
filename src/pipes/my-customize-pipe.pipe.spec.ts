import { MyCustomizePipePipe } from './my-customize-pipe.pipe';

describe('MyCustomizePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyCustomizePipePipe();
    expect(pipe).toBeTruthy();
  });
});
